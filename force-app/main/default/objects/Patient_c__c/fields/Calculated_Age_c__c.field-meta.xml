<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Calculated_Age_c__c</fullName>
    <externalId>false</externalId>
    <formula>IF(NOT(ISBLANK(Date_Of_Birth__c)), 
   YEAR(TODAY()) - YEAR(Date_Of_Birth__c) - 
   IF(OR(MONTH(TODAY()) &lt; MONTH(Date_Of_Birth__c), 
         AND(MONTH(TODAY()) == MONTH(Date_Of_Birth__c), DAY(TODAY()) &lt; DAY(Date_Of_Birth__c))), 
1, 0), 
   NULL)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Calculated_Age__c</label>
    <precision>18</precision>
    <required>false</required>
    <scale>0</scale>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Number</type>
    <unique>false</unique>
</CustomField>
